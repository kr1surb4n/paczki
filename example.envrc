#!/bin/bash

# project specific files
export NAMESPACE="wordpress"
export PROJECT="donate-payu"
export PROJECT_ROOT=`pwd`
export CONTEXT="wordpress, php"

# database specific
#export DB_CONNECTION=""
#export DB_SCHEMA=$NAMESPACE

# docker-compose default file
export COMPOSE_FILE="docker-compse.yml"

# set ssh key for git
export GIT_SSH_COMMAND='ssh -i ~/.ssh/id_rsa -o IdentitiesOnly=yes'
